import { ProjectImageLayoutEnum } from "./project-image-layout-enum";

export class ProjectImage {
  source: string;
  twoSources: string[];
  layout: ProjectImageLayoutEnum;
  description: string;
}
